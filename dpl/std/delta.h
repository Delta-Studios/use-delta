#ifndef DELTA_H
#define DELTA_H
#include <iostream>
#include <string>

namespace delta {
   //int puts(std::string args, ...);
   class Console;

   class str;
   class bin;
   class num;
}

#endif